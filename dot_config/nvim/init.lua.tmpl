-- [Rationale]: Dynamic provider injection based on XDG schema.
-- [Pre-condition]: Neovim isolated venv managed via setup-runtimes script.

-- Explicitly define the Python provider to prevent collision with project-local venvs.
vim.g.python3_host_prog = "{{ .paths.xdg_data }}/nvim/venv/bin/python"

-- Initialize the plugin manager and core configurations.
require("config.lazy")
